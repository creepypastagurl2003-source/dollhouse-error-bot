services:
  - type: web
    name: dollhouse-error-bot
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    plan: free
    envVars:
      - key: BOT3_TOKEN
        sync: false
      - key: PORT
        value: 10000
